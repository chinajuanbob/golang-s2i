#!/bin/bash

if [ "$(ls /tmp/artifacts/ 2>/dev/null)" ]; then
    mv /tmp/artifacts/go/* /go/
    mv /tmp/artifacts/.cache/* /.cache/
fi

cd /tmp/src/

GO111MODULE=on go build -v -o goapp

mkdir /app
cp goapp /app/
